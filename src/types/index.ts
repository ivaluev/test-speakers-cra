export type PropsSelected = {
  isSelected: boolean
}

export type PropsDelay = {
  delay: number
}

export type PropsDisabled = {
  disabled: boolean
}

export enum MouseEventType {
  NONE = 0,
  DOWN = 1,
  UP = 2,
}
